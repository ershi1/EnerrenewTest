[pytest]
testpaths = tests/test_suites
addopts =
    -v
    --alluredir=../reports/allure_results
    --cov=src
    --cov-report=html:../reports/coverage
markers =
    smoke: 冒烟测试
    regression: 回归测试